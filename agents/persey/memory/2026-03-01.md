# Memory — 2026-03-01

## Convex Deployment Discovery (Critical)
- `loyal-corgi-868` = **DEV deployment** of runner-music (not prod as previously believed)
- `flippant-quail-864` = **PROD deployment** of runner-music (deploymentType: prod, isDefault: true)
- All Convex functions are deployed to `flippant-quail-864.convex.cloud`
- Vercel env vars updated: `NEXT_PUBLIC_CONVEX_URL` and `EXPO_PUBLIC_CONVEX_URL` → `https://flippant-quail-864.convex.cloud`
- Both web + miniapp Vercel redeployments triggered

## Convex Env Vars (flippant-quail-864 / prod)
All set and confirmed:
- `TELEGRAM_BOT_TOKEN` ✓
- `SPOTIFY_CLIENT_ID` / `SPOTIFY_CLIENT_SECRET` ✓
- `RENDER_BASE_URL=https://render.srv1439489.hstgr.cloud` ✓
- `MAPBOX_API_TOKEN` ✓
- `DEV_AUTH_BYPASS=false` ✓
- `TELEGRAM_AUTH_MAX_AGE_SEC=900` ✓
- `TELEGRAM_SESSION_TTL_SEC=604800` ✓

## Convex CLI Quirk
- `CONVEX_DEPLOYMENT` in `.env.local` is IGNORED — CLI always picks the account default project
- `--env-file` flag correctly targets the right deployment but breaks auth (doesn't read `~/.convex/config.json`)
- Workaround used: just deploy to flippant-quail-864 directly (it IS the prod deployment)
- Deploy command: `cd ~/work/runmusic/backend/convex && bun x convex deploy --yes --typecheck=disable`

## RunMusic PM State (as of Sunday 1 March 2026)
- Wrapped V3 (3-slide) fully shipped
- PR #11 open (docs reconcile) — ready to merge
- AW-03 + AW-04 (crossfade wallpaper) — PRD written, not implemented
- Open bug: long-run duration jank (count-up animation, severity: medium)
- Wrapped KANBAN drift — not updated for V3 / Spotify SDK
- Lyrics coverage audit (LY-01) never started
- Video share flow (Option A: in-app preview modal) backend not started

## Vercel Project IDs
- Web app: `prj_yw3E3Zl4BZ9YsxySfwnptdM059Gw` → `https://web-one-fawn-50.vercel.app`
- Miniapp: `prj_juCN66P6JMsR2qBhMcdrhLvU70Qv`
- Vercel token: `[REDACTED_VERCEL_TOKEN]`
