# Daily Notes — 2026-03-04

## Wrapped scroll audio — preloading + dual-audio crossfade (00:00)

- Implemented stronger scroll-linked audio transitions to eliminate gaps:
  - `useSpotifyPlayback.ts` now caches Audio elements per preview URL (multi-audio strategy)
  - `crossfadeTo` loads next audio, waits for `canplaythrough` (fallback timeout), plays at vol 0, fades out current while fading in next
  - Added `preloadUrls(urls)` to warm up first 3 preview URLs
  - `RecapScreen.tsx` calls `preloadUrls(Object.values(trackPreviewMap).slice(0,3))` after Convex batch preview fetch
- Commit: `da59d58` — bot URL `?v=da59d58`
- Rationale: single-audio `src` swapping causes dead air due to buffering; preloading + dual audio makes transitions feasible.


---

## ACPX runtime enabled (00:18)

Michael asked to "install ACP".

Findings:
- ACP backend is provided by stock plugin **ACPX Runtime** (`acpx`) shipped with OpenClaw 2026.3.1.
- `openclaw plugins install @openclaw/acpx` failed (package not on npm) because it's already bundled as a stock extension.

Actions:
- Verified plugins list shows `ACPX Runtime (acpx)`.
- Enabled plugin via `openclaw plugins enable acpx`.
- Noted config did not have `acp` section initially; `openclaw config get acp.enabled` failed until set.
- Set config keys:
  - `acp.enabled=true`
  - `acp.dispatch.enabled=true`
  - `acp.backend=acpx`
  - `acp.defaultAgent=codex`
  - `acp.allowedAgents=["pi","claude","codex","opencode","gemini"]`

Gateway:
- `openclaw gateway status` shows gateway listening on `*:18789` (systemd user services unavailable; runs foreground/supervisor).

Next:
- Validate ACP sessions can now spawn (requires correct runtime config + backend health via `/acp doctor`).

---

## ACP installed/enabled confirmation (00:21)

Executed:
- `openclaw plugins enable acpx` (stock plugin)
- Set ACP config:
  - acp.enabled=true
  - acp.dispatch.enabled=true
  - acp.backend=acpx
  - acp.defaultAgent=codex
  - acp.allowedAgents=[pi, claude, codex, opencode, gemini]
- `openclaw plugins doctor` → no issues
- `openclaw config get acp.*` confirms values

Gateway restart via systemd not available (systemctl --user unavailable), but gateway is running and RPC probe ok.
Attempted `openclaw acp client` test; ACP client reported ready + newSession, then gateway disconnected (1005) likely due to non-interactive stdin/timeout test method.

Next verification:
- Run `/acp doctor` in a supported command surface, or run `openclaw acp client` interactively in a TTY to confirm full round-trip.

## Status confirmation (00:27)
- Confirmed to Michael: latest deploy for scroll+audio is `?v=da59d58`.
- Noted iOS constraint: first gesture required to start audio; after unlock, scroll crossfades should work.

## iOS issue: titles change, audio doesn't switch (00:27–00:35)
- Symptom: on iOS, active section changes update NowPlaying title/cover, but audio stays same.
- Hypothesis: `<audio>` deck load/play failing on iOS due to `crossOrigin="anonymous"` + iTunes preview URLs.
- Fix pushed: `33706d0` (bot URL `?v=33706d0`)
  - Removed `deck.crossOrigin` from dual-deck audio elements in `useSpotifyPlayback.ts`.
  - Improved audio error logs to include failing src.
  - Added `crossfadeToUrl: start` log.

## Vercel deploy verification (00:47)
- Confirmed Vercel production deployment is updated to latest `main` commit SHA `33706d08` (PROMOTED/READY).
- Production URL `run-music-telegram-miniapp.vercel.app/?v=33706d0` served from edge (x-vercel-cache HIT).
- Reminder: Telegram iOS WKWebView caching still requires killing Telegram to reload.

## iOS overlap bug fix (00:59)
- Report: tracks switch but no fade-out; multiple tracks audible simultaneously.
- Root cause: fast scroll cancels fade timers via transitionId mismatch, leaving old deck playing.
- Fix pushed: `3c26f10` (bot URL `?v=3c26f10`)
  - In `fadeDeck`, when transitionId mismatch and target==0, force stop deck (volume=0, pause, reset currentTime).
  - In `crossfadeToUrl`, added hard-stop of any previous deck before starting next transition to prevent overlap.

## Wrapped segmented map mock PRD + PR (02:16)
- Wrote PRD: `docs/prd-wrapped-map-song-segments-mock.md`
- Added TODO to `docs/pm/backlog.md`
- Created branch: `feature/segmented-route-map-mock`
- Implemented Mapbox segmented route mock + labels:
  - `apps/telegram/src/components/RouteMapSegmented.web.tsx`
  - `apps/telegram/src/components/RouteMapSegmented.tsx` (re-export)
- Fixed TS issues in GeoJSON typing + vendor CSS style property.
- Commit: `9f6aef8` — feat(wrapped): mock segmented route map by soundtrack
- PR opened: https://github.com/Michailbul/run-music/pull/16

## PR #16 merged — segmented route map mock (11:34)
- PR #16 merged: https://github.com/Michailbul/run-music/pull/16
- Merge method: squash/auto + delete branch
- Adds RouteMapSegmented.web.tsx + PRD + backlog TODO

---

## PRD + delegation: Resizable Crons layout (13:12)

Michael requested: craft detailed PRD and delegate to Codex to make every major section on Crons page resizable with great UX.

Wrote PRD:
- `/root/michael/prd-resizable-crons.md`

PRD requirements:
- Left cron list resizable (min 180px, max 420px), persisted `agentHub.crons.listWidth`
- Right skills rail resizable (min 260px, max 520px), persisted `agentHub.crons.skillsWidth`
- Collapsed skills rail fixed at 40px; reopen restores last width
- Visible 8px handles, coral hover, col-resize cursor
- Prevent text selection during drag (body userSelect none), set cursor while dragging
- Layout reserves space for fixed right rail via padding-right math
- Implement via reusable hook `client/src/lib/useResizablePane.ts`
- Client-only changes, must pass `npm run build`

Delegated to Codex CLI:
- Codex PID 1086817
- Log: `/tmp/codex-resize-prd.log`

## Integrated wrapped design polish into main (13:12)
- Found branch: `origin/codex/wrapped-scroll-polish`
- Cherry-picked commit `ea6127a` (wrapped: polish scrollable recap sections)
- Created branch: `feature/integrate-wrapped-polish`
- Opened PR #17 and merged to main (squash/auto)
  - PR: https://github.com/Michailbul/run-music/pull/17
- Changes touch WrappedScrollView + SectionRun/Soundtrack/Streak/Vibe + NowPlayingBar.
