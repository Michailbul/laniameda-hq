
- Reminder / line to keep: "Sun doesn’t care whether the grass appreciates its rays."
- Saved Instagram color palette post `https://www.instagram.com/p/DVyqQD4D5GA/` into laniameda.gallery under `designs` pillar using ingest key `instagram:DVyqQD4D5GA`; limitation: without logged-in Instagram session only the first slide/og:image was accessible, not the full carousel.
- Correction / rule after user feedback: when Michael says to save a post/carousel/palette to gallery, save the **actual user-referenced asset(s)** (direct file, actual slide images, or exact screenshots of each slide). Do **not** silently substitute metadata (`og:image`, caption summary, representative cover) and mark it done. If the true asset is blocked, report the block and ask for the minimum unblock step instead of ingesting a surrogate.
- Browser failure investigation for the Instagram save incident: Browser-Use Cloud was attempted but failed first due to a local script mistake importing `zod` in an ad-hoc Node inline script, then timed out on a second run. Host OpenClaw browser path was also unhealthy at check time (`browser status`: not running; `browser profiles`: timed out and advised gateway restart). No `BROWSER_USE_PROFILE_ID` was configured, so authenticated Instagram extraction via cloud profile was not available either.
