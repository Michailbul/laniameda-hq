- User set a new hard rule after Instagram/gallery failures: **never finish extraction/ingestion tasks halfway**. KPI is actual success, not partial progress. If something fails, keep iterating through fixes and alternate paths until the requested deliverable is achieved; only surface a blocker after viable recovery paths are exhausted.
- Browser priority for authenticated Instagram/social extraction: **(1) Browser-Use Cloud with logged-in profile, (2) Brave/Chrome via Michael's Mac node**. Do not fall back to metadata/text-only completion while these paths are still viable to debug.
- Browser-Use Cloud profile configured globally: `BROWSER_USE_PROFILE_ID=3e7944ef-d50e-4d13-b3bd-8efe953d4662`.
- Practical Browser-Use recovery lessons from the Instagram extraction work: SDK imports may require the global path `file:///usr/lib/node_modules/browser-use-sdk/dist/v3.js`; SDK `run()` can time out at 300000ms, so manual session create/poll or direct REST API is a valid fallback; Instagram CDN media URLs may fail outside the authenticated browser session, so browser workspace screenshots/downloaded files are the proper recovery path for saving the actual slide assets.
- User clarified another extraction rule: for carousel saves, prefer the original slide images themselves whenever possible; do not create screenshots if the originals can be saved. Only use screenshots as fallback.
- User clarified image filtering rule: when extracting prompt carousels, ingest only images directly relevant to the prompt set. Ignore thumbnails, profile images, suggested posts, and unrelated surrounding media. If relevant originals do not exist/access fails, state that clearly and then use the best fallback.
